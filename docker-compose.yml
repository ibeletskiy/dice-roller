services:
  dice-roller-bot:
    build: .
    container_name: dice-roller-bot
    env_file:
      - .env
    environment:
      DB_PATH: ${DB_PATH:-/data/dnd_bot.db}
    volumes:
      - ./data:/data
    restart: unless-stopped
